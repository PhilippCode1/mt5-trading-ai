# AUDIT — mt5_trading_ai · 2026-08-11

> **Befangener Selbst-Report.** Dieser Audit wurde von demselben Agenten ausgeführt, der das
> System gebaut hat. Nach Teil 0.4 / Teil D des Rasters (v2) ist er **vorläufig** und
> **muss** durch einen unabhängigen Prüfer gegen die Rohdaten gegengeprüft werden, bevor eine
> Freigabeentscheidung darauf gestützt wird. `independent_of_builder = false`.

## 1. Kopf

| | |
|---|---|
| Zielbaum | `C:\Users\Acer\mt5_trading_ai` · Repo `PhilippCode1/mt5-trading-ai` |
| Commit | `8a9f19f` |
| Umgebung | Windows 11, Python 3.11, pytest 8.3.4, ruff, mypy --strict |
| Raster | v2 (Bewertungsraster und Audit-Masterprompt) |
| Auditor | bauender Agent — **Selbst-Report, vorläufig** |

## 2. Die drei Zahlen

| Note | Wert | Bemerkung |
|---|---|---|
| **Handelsreife** | **1,1** | nie gehandelt, keine validierte Strategie, kein Backtest |
| **Betriebsreife** | **1,7** | starker Testkern, aber kein Live-/Demo-Beleg, kein close-all-Notaus, keine CI |
| **Freigabenote** | **1,1** | = MIN(Handelsreife, Betriebsreife), **vorläufig** |

## 3. Ausgelöste K.-o.-Regeln

- **Kein close-all-Notaus, 6.6 ungetestet** → Gesamtnote höchstens 3. *(Korrektur nach
  Gegenprüfung: ein getestetes HALT-Tor existiert sehr wohl — `risk/limits.py`
  `TradingState.HALTED` sperrt neue Orders und löst sich nicht selbst; der ursprüngliche Grep
  suchte fälschlich `kill_switch` mit Unterstrich, der Code schreibt „Kill-Switch". Aber 6.6
  verlangt zusätzlich einen getesteten „schließt-alles"-Befehl: `grep -rniE
  "close_all|flatten" mt5_trading_ai tests` → leer, und kein Live-Lauf. 6.6 bleibt damit
  ungetestet.)*
- **Kein Out-of-Sample-Nachweis (1.2)** → Handelsreife höchstens 2.
- **Keine Live-/Demobelege** → Bereiche 5, 6, 7, 9 „nicht bewertbar".
- **Befangener Selbst-Report ohne unabhängige Gegenprüfung** → Freigabenote vorläufig.
- Nicht ausgelöst: „Testfehlschläge > 0" (0 Fehlschläge gemessen).

## 4. Bestandsaufnahme (Schritt 1, gemessen)

| Größe | Wert |
|---|---|
| Produktionsmodule (`mt5_trading_ai/*.py` ohne `__init__`) | 18 |
| Zeilen Produktionscode | 4.112 |
| Testdateien / Testfunktionen | 18 / 190 |
| Zeilen Tests | 2.628 |
| Werkzeuge | 3 (`gen_docs`, `check_docs_claims`, `mt5_smoke`) |
| Einsprungpunkte | 3 (nur in `tools/`) |
| Dienste | 1 Paket, 6 Unterpakete (7 `__init__.py` inkl. Wurzelpaket) — **keine** Dienstarchitektur |
| Entscheidungskern (Signal→Order) | **nicht vorhanden** (keine Signal-/Strategiefunktion gefunden) |
| Backtest-Engine | **nicht vorhanden** (nur `backtest/splits.py`, Hilfsfunktionen) |
| Alt-Baum `bitget-btc-ai` | ~237.000 Zeilen (laut Doku; nicht neu gemessen — Größe) |

**Vorhersage (geschätzt, vor der Benotung):** Aus dem Stand — nie gehandelt, keine Strategie,
kein Backtest, kein Notaus, keine CI — war Handelsreife 1 und Betriebsreife 1–2 zu erwarten.
Das Ergebnis (1,0 / 1,7) liegt im vorhergesagten Rahmen; kein Verdacht auf Aufwärts-Messfehler.

## 5. Notentabelle (Schritt 6)

### Block I — Handelsreife

| Nr | Note | Art | Beleg / Grund |
|---|---|---|---|
| 1.1 Hypothese | 1 | gemessen | keine datierte, falsifizierbare Marktannahme je Klasse gefunden |
| 1.2 Out-of-Sample | 1 | gemessen | kein Walk-Forward-Ergebnis (keine Strategie); **K.-o. Bereich 1 ≤ 2** |
| 1.3 Mehrfachtest | 2 | gemessen | Versuchsregister `gates/trials.py` vorhanden, aber ohne Strategie-Einträge |
| 1.4 Stichprobe | 1 | gemessen | 0 Trades je Klasse (nie gehandelt) |
| 1.5 Kostenrealismus | 1 | gemessen | keine Sensitivität (keine Strategie) |
| 1.6 Regimewechsel | 1 | gemessen | keine Ergebnisse |
| 1.7 Nullhypothese | 1 | gemessen | kein Vergleich gegen Zufall/Halten |
| **Bereich 1** | **1,1** | | Mittel 8/7 = 1,143; K.-o. 1.2/1.4 ist Deckelung bei 2 (greift nicht, da < 2) |
| 3.1–3.5 Backtest | 1 | gemessen | keine Backtest-Maschine; `splits.py` ist ein Baustein, kein Lauf |
| **Bereich 3** | **1,0** | | Determinismus (3.3) nicht bewertbar |

### Block II — Betriebsreife

| Bereich | Note | Art | Beleg / Grund |
|---|---|---|---|
| 2 Daten | 2 | gemessen | Datenqualitäts-Tor `data/quality.py` getestet, aber **kein realer Datenstrom**, kein Rohdaten-Hash |
| 4 Analystenschicht | **N/A** | m. Beleg | kein Sprachmodell im Entscheidungspfad (grep: keine Modell-/LLM-Aufrufe im Kern) → Gewicht entfällt |
| 5 Ausführung/MT5 | 1 | n. bewertbar | kein Live-/Demo-Beleg (A.2). Logik gegen Fake-Terminal getestet, real nie gelaufen |
| 6 Risiko | 1 | n. bewertbar | A.2 (kein Live/Demo). HALT-Tor (Kill-Switch-Gate) + Hebelklammer als Logik getestet, aber 6.6 ungetestet: kein close-all-Befehl, kein Live-Lauf |
| 7 Betrieb | 1 | n. bewertbar | keine Prozessüberwachung, Alarmierung, Zustandssicherung; kein Live-Beleg |
| 8 Code/Tests | 4 | gemessen | 0 Fehlschläge (8.2 ↑), ruff prüft 46/46, mypy 28/46, Mutationsprobe 6/7; aber keine CI (8.5), kein Lockfile (8.6) |
| 9 Beobachtbarkeit | 1 | n. bewertbar | kein Handelstagebuch (keine Trades); `trials.py` anhängend (9.4 Ansatz) |
| 10 Recht/Steuer/Halal | 2 | gemessen | ESMA-Hebelklassen im Code (10.2); Broker/Steuer/Halal/Lizenz **nicht** adressiert; **keine LICENSE-Datei** |
| 11 Doku/Übergabe | 4 | gemessen | overview.html überwiegend belegt, 2 Drift-Stellen; `VERLUST.md`/`PROGRESS.md` als Entscheidungsprotokoll (11.3 ↑); kein Störfallhandbuch (11.2) |
| 12 Freigabeweg | 1 | gemessen | Live-Freigabe definiert, aber keine numerischen Tore, kein Demo-Dauerlauf, keine Abschaltkriterien, keine Kapitalstaffel |

**Rechnung (nach Gegenprüfung korrigiert):**
```
Bereich 1     = (1+1+2+1+1+1+1) / 7 = 8/7 = 1,143   (Zwischenwert nicht gerundet, A.1)
Handelsreife  = (1,143×22 + 1×12) / 34              = 37,14/34 = 1,092 → 1,1
Betriebsreife = (2×8 + 1×12 + 1×14 + 1×7 + 4×7 + 1×4 + 2×5 + 4×2 + 1×1) / 60 = 100/60 = 1,67 → 1,7
                (Bereich 4 = N/A → Gewicht 6 aus dem Nenner: 66 → 60)
Freigabenote  = MIN(1,092; 1,667) = 1,092 → 1,1   (vorläufig, Selbst-Report)
```

## 6. Wirksamkeitsproben (Schritt 3)

| Probe | Ergebnis | Beleg |
|---|---|---|
| a) Zukunftszugriff im Entscheidungspfad | **nicht bewertbar** | kein Entscheidungspfad/Backtest vorhanden |
| b) CI-/Doku-Sperre verletzen | **wirksam (lokal)** | `check_docs_claims` exit=1 bei „produktionsreif/10/10", nach Rücknahme grün. **Aber keine CI erzwingt sie** |
| c) Backtest zweimal, gleicher Seed | **nicht bewertbar** | keine Backtest-Maschine |
| d) MT5-Verbindung trennen | **nicht bewertbar** | keine Anbindung (kein `MetaTrader5`, kein Terminal) |
| e) Tagesverlust-Abschalter | **Logik belegt, System nicht** | `limits.py` getestet + Mutation gefangen; kein laufendes System zum Stoppen |
| f) Notaus | **Note 1** | HALT-Tor („sperrt neue Orders") getestet, aber **kein „schließt-alles"-Befehl** (`grep close_all\|flatten` leer) und kein Live-Lauf → 6.6 ungetestet |
| g) 10 (hier 7) bewusste Fehler | **6 von 7 gefangen (86 %)** | 6× rot, 1× durchgerutscht: **Purge-Entfernung in `splits.py` fällt nicht auf**. Rücknahme belegt: `244 passed`, Baum sauber |

## 7. Historienprüfung (Schritt 4)

- **Zielbaum** (18 Commits, alle Branches, `git log --all -p`): **keine** Zugangsdaten,
  Schlüssel oder IPs. Einziger Treffer: `self._password = password` — ein Parameter, kein Wert.
- **Alt-Baum `bitget-btc-ai`** (nur aktueller Stand via `git grep`; Vollhistorie wegen Größe
  nicht gescannt — als Grenze vermerkt): ein **Test-Fixture** mit einem offensichtlich
  gefälschten Schlüssel (`tests/llm_eval/test_guardrails.py`: `sk-abcdefghijklmnop…`), das die
  Redaction testet — kein echtes Geheimnis. Übrige Treffer sind Übersetzungstexte/Kommentare.

## 8. Dokumentationsabgleich (Schritt 5)

overview.html, geprüfte Tatsachenaussagen: **7 belegt, 2 widerlegt** von 9 geprüften.

| Aussage | Ergebnis | Bezug |
|---|---|---|
| „18 Module" | belegt | gemessen 18 |
| „4.112 Zeilen" | belegt | gemessen 4.112 |
| „190 Testfunktionen" | belegt | gemessen 190 |
| „244 Tests grün" | belegt | `244 passed` |
| „0 ruff/mypy-Fehler" | belegt | ruff pass, mypy Success |
| „drei Live-Sperren in Reihe" | belegt | Live-Freigabe + allow_write + Demo-Abbruch im Code |
| Modul-Zeilen (818/276/…) | belegt | Stichprobe stimmt |
| **„privat"** | **widerlegt** | tatsächliche Sichtbarkeit **PUBLIC** |
| **„17 Commits"** | **widerlegt** | tatsächlich **18** |

## 9. Die fünf schwersten Befunde

| # | Befund | Beleg | Folge, wenn er bleibt | Aufwand (grob) |
|---|---|---|---|---|
| 1 | Nie gehandelt, keine validierte Strategie, kein nachgewiesener Vorteil | Bereich 1 = 1,0; 0 Trades | Das System kann keinen Ertrag bezeugen; Betrieb hieße blind handeln | groß (Strategie + Validierung) |
| 2 | Kein „schließt-alles"-Notaus (6.6 ungetestet) | HALT-Tor getestet, aber `grep close_all\|flatten` leer + kein Live-Lauf | Positionen lassen sich im Fehlerfall nicht per Befehl schließen; K.-o. auf Gesamtnote 3 | mittel |
| 3 | Keine Backtest-Maschine, kein Determinismus-Nachweis | Bereich 3 = 1,0 | Jede spätere Strategiekennzahl ist unbelastbar | groß |
| 4 | Keine CI erzwingt die (wirksamen) Sperren | Probe b, kein `.github/workflows` | Eine Regression kann unbemerkt landen; Sperren sind nur bei manuellem Lauf aktiv | klein |
| 5 | Doku-Drift + eine ungefangene Mutation | „privat"→public, „17"→18; Purge in `splits.py` | Übergabe-Aussagen veralten still; ein Leckage-Bug im Splitter fällt nicht auf | klein |

## 10. Fundliste für danach (nicht im Audit bearbeitet)

- `docs/overview.html`: „privat"/„17 Commits" nachziehen (Drift).
- `tests/test_splits.py`: Testfall ergänzen, der das Entfernen der Purge fängt (Mutation g7).
- `.github/workflows/`: CI anlegen, die `pytest`, `ruff`, `mypy`, `gen_docs --check`,
  `check_docs_claims` bei jedem Push erzwingt.
- Keine `LICENSE`-Datei im (jetzt öffentlichen) Repo.
- Kein Lockfile (Abhängigkeiten nicht festgenagelt).
- Kein „schließt-alles"-Notaus-Befehl (nur HALT-Tore, die neue Orders sperren).

## 11. Was ich nicht messen konnte und warum

- **Handelsvorteil, Out-of-Sample, Kostensensitivität** — keine Strategie/Backtest vorhanden.
- **Live-Ausführung, Slippage, Reconnect, Notaus in Aktion** — keine MT5-Anbindung/kein Lauf.
- **Determinismus (3.3)** — keine Backtest-Maschine.
- **Vollhistorie des Alt-Baums auf Geheimnisse** — Größe; nur aktueller Stand gescannt.
- **Unabhängigkeit** — dies ist ein Selbst-Report; eine erste maschinelle Gegenprüfung durch
  drei separate Prüfagenten ist erfolgt (Abschnitt 12); eine menschliche Bestätigung vor jeder
  Freigabe bleibt Pflicht.

## 12. Unabhängige Gegenprüfung (Teil-D-Pflicht)

Drei unabhängige Prüfagenten haben nachgemessen; jede gemeldete Diskrepanz wurde erneut
gegengeprüft. Von 7 gemeldeten wurden **3 bestätigt und oben eingearbeitet:**

1. **Kill-Switch-Aussage falsch (mittel, factual_error).** Der ursprüngliche Grep suchte
   `kill_switch` (Unterstrich); der Code schreibt „Kill-Switch". Ein getestetes HALT-Tor
   existiert (`risk/limits.py`, `TradingState.HALTED`, kein Selbst-Reset). Korrigiert: es fehlt
   der *close-all*-Befehl und der Live-Lauf → 6.6 bleibt ungetestet, Note und K.-o. unverändert.
2. **Aggregation zu tief (klein, deflated).** Bereich 1 = 8/7 = 1,143 (Zwischenwert nicht
   runden, A.1); die K.-o.-Regel ist eine Deckelung bei 2, kein Boden. Handelsreife und
   Freigabenote steigen von 1,0 auf **1,1**.
3. **Unterpakete zu hoch (klein, inflated).** 6 statt 7 (das Wurzelpaket war mitgezählt).
   Ohne Notenwirkung.

Vier weitere gemeldete Punkte wurden **nicht** bestätigt. Bemerkenswert: die Korrekturen
gingen in **beide** Richtungen (eine zu tiefe Note, eine überharte Falschaussage) — kein
einseitiges Schönen. Der Befund bleibt: ein gut getesteter Sicherheitskern, aber Handels- und
Betriebsreife niedrig, weil nichts gehandelt und nichts live gelaufen ist.

---

*Der Bericht darf schlecht ausfallen. Freigabenote 1,1 (vorläufig) ist das ehrliche Ergebnis:
ein sauber gebauter, gut getesteter Sicherheitskern, der noch nichts handelt und dessen
Betriebs- und Handelsreife erst mit realen Läufen und einer validierten Strategie entstehen
kann. Bismillah.*
